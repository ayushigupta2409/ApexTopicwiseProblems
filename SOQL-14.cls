/*
Insert an Account along with 1 related Contact and 1 related Opportunity.
*/
public class ques1 {
    public static void fun(){
        Account a = new Account(name='account_new');
        insert a;
        //Id is a field that is autogenerated once record is inserted into the database.
        Contact c = new Contact(Lastname='Contact_new',AccountId=a.Id,phone='123456');
        Opportunity o = new Opportunity(name='opportunity_new',StageName='Prospecting',CloseDate=system.today(),AccountId=a.Id);
        insert c;
        insert o;
   }
}
